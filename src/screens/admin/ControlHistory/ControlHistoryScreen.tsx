import { SafeAreaView, Text } from "react-native";

export default function ControlHistoryScreen() {
    return (
        <SafeAreaView style={{ flex: 1, padding: 16 }}>
            <Text>Control History Screen</Text>
        </SafeAreaView>
    );
}